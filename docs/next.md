# 后续改进计划

## 已完成

- [x] 临时会话: `--temp` 参数，使用 `tmp_` 前缀 ID，切换/新建会话时自动清理
- [x] 会话命令提升: `session current/switch/new` 独立管理当前活跃会话
- [x] 短 ID: Docker 风格显示（前 8 字符），支持前缀匹配输入

## 待定

- 增加集成测试和 golden tests
- 帮助文本优化
- Profile 管理完善